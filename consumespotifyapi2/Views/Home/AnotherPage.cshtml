@model IEnumerable<consumespotifyapi2.Models.Playlist>

@{
    ViewData["Title"] = "Another page";
}

<div class="row">
    @foreach (var item in Model)
    {
        <div class="card m-1" style="width: 350px;">
            <div class="card-body">
                
                <p1>@Html.DisplayFor(modelItem=>item.Name)</p1>
                
            </div>
        </div>
    }
</div>


<p>Do whatever.</p>